# Dependencies
node_modules

# Renderer build output
dist
dist-ssr
*.tsbuildinfo

# electron-builder packaging output
release/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Local overrides
*.local

# .env.electron is NOT ignored — it is what points the packaged build at the
# deployed API instead of localhost:3000. Without it in the repo, CI ships an
# app that cannot reach the backend.
.env
.env.*
!.env.electron

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
